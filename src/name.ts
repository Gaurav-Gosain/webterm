/**
 * The one place the package name appears in the source tree.
 *
 * `PACKAGE_GLOBAL` is the UMD global; `CSS_PREFIX` is the stem of every CSS
 * custom property and class name the package emits. Renaming the package is a
 * change to these two lines plus the `name` field in package.json.
 */
export const PACKAGE_GLOBAL = 'WebTerm';
export const CSS_PREFIX = 'webterm';
